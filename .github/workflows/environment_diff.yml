# This is a basic workflow to help you get started with Actions

name: Environment Diff CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for changes to the environment_diff directory
  push:
    paths: ["environment_diff/**"]
  pull_request:
    branches: ["main"]
    paths: ["environment_diff/**"]


  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  lint:
    name: "ruff lint and format checks"
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Check formatting with ruff
        run: |
          uv run --project environment_diff --extra dev ruff format --check environment_diff

      - name: Lint with ruff
        run: |
          uv run --project environment_diff --extra dev ruff check environment_diff

      - name: Type check with mypy
        run: |
          uv run --project environment_diff --extra dev mypy environment_diff --exclude "build"

  test:
      name: "run pytest"
      # The type of runner that the job will run on
      runs-on: ubuntu-latest

      # Steps represent a sequence of tasks that will be executed as part of the job
      steps:
        # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
        - uses: actions/checkout@v4
        - uses: actions/setup-python@v5
          with:
            python-version: "3.10"

        # adapted from GitHub docs for building and testing Python
        # https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python
        # and strategies used in Python samples
        # https://github.com/GoogleCloudPlatform/python-docs-samples/blob/main/noxfile-template.py
        - name: Install dependencies
          # install pytest, module dependencies and tool itself
          run: |
            python -m pip install --upgrade pip
            pip install pytest
            pip install -r environment_diff/requirements.txt
            pip install environment_diff/
        - name: Test with pytest
          run: |
            pytest environment_diff/
