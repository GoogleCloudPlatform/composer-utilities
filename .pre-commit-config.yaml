repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.16.4
    hooks:
      # Run the linter.
      - id: ruff-check
        args: [ --fix ]
        files: ^cicd/
      # Run the formatter.
      - id: ruff-format
        files: ^cicd/
